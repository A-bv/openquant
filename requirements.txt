# OpenQuant dependencies — pinned to the exact versions the test suite passes
# against (CI re-proves them on every push). Bump deliberately, with the tests
# as the gate, not implicitly at install time.

# API
fastapi==0.136.3
uvicorn==0.47.0
python-multipart==0.0.29

# Data
yfinance==1.3.0
pandas==2.3.3
numpy==2.3.4
requests==2.32.3

# Math and optimisation
scipy==1.16.3
scikit-learn==1.7.2     # Ledoit-Wolf covariance

# Charts
plotly==6.7.0

# Config and security
python-dotenv==1.2.2

# Testing
pytest==9.0.3
pytest-cov==7.1.0

# Code quality
black==26.5.1
