# Environment
.env
*.env

# Local reference only — EPFL/Berk-DeMarzo course handouts (copyrighted). Never publish.
infoSource/

# Cache and data — fetched caches only. Blanket *.json/*.csv rules used to live
# here and silently kept the frontend manifest, its lockfile and the backtest
# results out of git entirely; ignore by location, never by extension.
data/cache/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.coverage
htmlcov/
dist/
build/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# OS
.DS_Store
Thumbs.db

# Screenshots and debug scripts (never commit these)
/*.png
screenshot_*.py

# Frontend build output and local env
frontend/dist/
frontend/node_modules/
frontend/.env.local

# IDE and local tooling
.vscode/
.idea/
*.swp
.claude/
